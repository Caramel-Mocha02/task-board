# CLAUDE.md

このファイルは、このリポジトリで作業する際に Claude Code（claude.ai/code）が参照するガイダンスです。

## プロジェクト概要

**task-board** — タスク管理ボードアプリケーション。

> 現状このリポジトリはまだ空です。実装が進んだら、以下の項目を随時更新してください。
> - 技術スタック（言語 / フレームワーク / 主要ライブラリ）
> - ディレクトリ構成と各領域の役割
> - アーキテクチャ上の重要な決定事項

## 開発コマンド

<!-- 実装が始まったら、実際のコマンドに置き換えてください。例:
- セットアップ: `npm install`
- 開発サーバー: `npm run dev`
- ビルド: `npm run build`
- テスト: `npm test`
- Lint: `npm run lint`
-->

（未定義 — セットアップ後に追記）

## Git 運用ルール

**コードを変更するたびに、必ず GitHub へプッシュすること。** 変更をローカルに溜め込まず、意味のある単位ごとにコミットし、その都度リモートへ反映します。

### 基本フロー

作業のたびに以下を実行します。

```bash
git add -A
git commit -m "<変更内容を表す簡潔なメッセージ>"
git push
```

### ルール詳細

- **こまめにコミット・プッシュする** — 一つの論理的な変更（機能追加・修正・リファクタなど）が完了したら、その都度コミットしてプッシュする。複数の無関係な変更を一つにまとめない。
- **プッシュ漏れを防ぐ** — 作業の区切りごとに `git status` で未プッシュ・未コミットの変更が残っていないか確認する。
- **コミットメッセージ** — 何を・なぜ変更したかが分かるように書く。
- **ブランチ運用** — `main` へ直接作業する場合も、変更後は必ず `git push` でリモートを最新に保つ。フィーチャーブランチを使う場合は、プッシュ後に Pull Request を作成する。

### 初期セットアップ（未実施の場合）

このリポジトリはまだ Git 初期化されていません。作業開始前に以下を実行してください。

```bash
git init
git branch -M main
git remote add origin <GitHub リポジトリの URL>
git add -A
git commit -m "Initial commit"
git push -u origin main
```
